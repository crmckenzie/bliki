@model Bliki.Models.Wiki.WikiPageViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>@Model.Title</h1>
<div class="container">
    <div class="row">
        @Html.ActionLink("Edit", "Edit", new {id = Model.Title})
    </div>
    <div class="col-md-12">
        @Html.Raw(Model.Body)
    </div>
</div>